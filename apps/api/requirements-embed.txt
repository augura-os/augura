# 本地 embedding 后端依赖（fastembed + onnxruntime，约 +150MB）。
# 官方镜像（infra/docker/api.Dockerfile）默认安装本文件，向量召回开箱即用；
# 源码部署：pip install -r requirements-embed.txt。
# embedding_backend=local 但缺依赖时自动降级文本兜底（见 app/services/embedding.py）。
fastembed==0.8.0
