[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Start lenient (E/F/I); tighten over time — see CONTRIBUTING.md.
select = ["E", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["E501"]
